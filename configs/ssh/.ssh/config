# GitHub は 443 経由で接続する。
# 鍵の選択は ~/.gitconfig / ~/.gitconfig-techouse 側で行う。
Host github.com
  HostName ssh.github.com
  Port 443
  User git

# マシン固有の host 設定や override はここに分離する。
Include ~/.ssh/config.local
