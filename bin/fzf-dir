#!/usr/bin/env sh
# ディレクトリをfzfで選択し、パスをstdoutへ出力
if command -v fd >/dev/null 2>&1; then
  fd --type d --hidden --follow --exclude .git
else
  find . -type d -not -path '*/.git/*'
fi | fzf \
  --prompt='dir> ' \
  --preview 'ls {}'
